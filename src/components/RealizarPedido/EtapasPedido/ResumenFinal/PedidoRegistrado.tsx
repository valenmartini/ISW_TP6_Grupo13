import React from 'react';
import { EtapaProps } from '../../RealizarPedido';

export const PedidoRegistrado = ({ datosEstados, setDatosEstados, avanzarEtapa, etapa }: EtapaProps) => {
  return (
    <React.Fragment>
      Pedido Confirmado
    </React.Fragment>
  );
};

export default PedidoRegistrado;
